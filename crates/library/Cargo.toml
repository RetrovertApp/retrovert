[package]
name = "retrovert-library"
version.workspace = true
repository.workspace = true
edition = "2021"
license = "MIT"
publish = false
description = "The music library: a directory walk and the row each song becomes once its decoder has described it"

[dependencies]
retrovert-host.workspace = true
serde.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
